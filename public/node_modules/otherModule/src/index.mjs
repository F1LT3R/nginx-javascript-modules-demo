console.log('otherModule was loaded!');

const deepModulePath = `${import.meta.url}/node_modules/deepModule`;

const otherModule = {
    message: 'otherModule was loaded!',
    importDeepDynamicModule: () => new Promise((resolve, reject) => {
        import(deepModulePath)
            .then(deepModule => {
                resolve(deepModule.default);
            })
            .catch(error => reject(error));
        })
};



export default otherModule;
