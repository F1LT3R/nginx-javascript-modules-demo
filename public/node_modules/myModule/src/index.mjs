console.log(`myModule was loaded!`);

import otherModule from '/node_modules/otherModule';

(async () => {
    const deepDynamicModule = await otherModule.importDeepDynamicModule();
    console.log(deepDynamicModule);
})();